<h1>Hello Student</h1>

<ul>
<%   @students.each do |student| %>
    <li>
    <a href="/students/<%= student.id %>">
    <%= student.firstname %>
  <%= student.lastname %>
  <%= student.email %>
  <%= link_to 'view', student %>
  </a>
    </li>
<% end %>
</ul>